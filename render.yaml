services:
  - type: web
    name: focusflow-smart-timer
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_MODEL
        value: gemini-2.5-flash
